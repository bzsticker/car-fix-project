# BUG TRACKER - รายงานการตรวจสอบระบบและดักจับข้อผิดพลาด (Den Modify Management System)

Audit Date: **2026-06-01**  
Author: **Antigravity (AI System Auditor)**  
Status: **System-Wide Audit Completed**

---

## 1. บทสรุปผู้บริหาร (Executive Summary)

จากการตรวจสอบโครงสร้างพื้นฐานระบบ (Full System Audit) ครอบคลุมถึงโครงสร้างฐานข้อมูล (Database Schema), นโยบายความปลอดภัยของแถวข้อมูล (RLS), ตัวประมวลผลคำสั่ง API (API Handlers), มิดเดิลแวร์การควบคุมทิศทาง (Next.js Middleware) และตรรกะทางธุรกิจในระดับโค้ด (Business Logic) ทางเราได้ตรวจพบช่องโหว่ความปลอดภัยระดับวิกฤต (Critical Security Leaks) ปัญหาความไม่สอดคล้องของข้อมูลระดับสูง (Data Consistency Problems) และฟังก์ชันที่ยังขาดหายไปตามข้อกำหนดใน V3 Specification ดังรายละเอียดในเอกสารนี้

**รายงานผลการตรวจเช็กและรันคำสั่งทดสอบ:**
- `npm run lint`: **ผ่านสะอาด (Zero errors/warnings)**
- `npx tsc --noEmit`: **ผ่านสมบูรณ์แบบ**
- `npm test`: **ผ่านการตรวจสอบ mock unit tests ทั้งหมด**

---

## 2. รายการข้อผิดพลาดและช่องโหว่ด้านความปลอดภัยระดับวิกฤต (Critical Bugs & Security Vulnerabilities)

### 🚨 BUG 1: ช่องโหว่การข้ามผ่านการตรวจสอบความปลอดภัยของมิดเดิลแวร์ (Middleware Routing Mismatch & RBAC Bypass)
* **โฟลเดอร์ที่มีปัญหา:** `src/middleware.ts`
* **รายละเอียดปัญหา:**  
  ในไฟล์ `middleware.ts` มีการป้องกันสิทธิ์เข้าถึงพาธโดยใช้เงื่อนไขตรวจสอบพาธขึ้นต้นด้วย `/dashboard` (เช่น `pathname.startsWith("/dashboard")`, `pathname.startsWith("/dashboard/owner")` เป็นต้น)  
  **อย่างไรก็ตาม** ในโครงสร้างไดเรกทอรีของโปรเจกต์ โฟลเดอร์แดชบอร์ดใช้ชื่อ `(dashboard)` (ครอบด้วยวงเล็บ) ซึ่งใน Next.js เรียกว่า **Route Group** ซึ่งจะถูกละเว้น (Excluded) จาก URL บนบราวเซอร์จริง ส่งผลให้ URL ในระบบจริงไม่มีคำว่า `/dashboard` แต่นำหน้าด้วย `/branch`, `/owner`, หรือ `/tech` โดยตรง (เช่น `/branch/reports` แทนที่จะเป็น `/dashboard/branch/reports`)  
  ทำให้เงื่อนไขตรวจสอบความปลอดภัยของมิดเดิลแวร์ทั้งหมด**ไม่เคยทำงานเลย (Never Evaluated)**
* **ผลกระทบ:**  
  1. ผู้ใช้ที่ยังไม่ได้ล็อกอิน (Unauthenticated) สามารถเข้าถึงหน้ากากโครงร่างแดชบอร์ดหลักของทุกบทบาทได้โดยตรงผ่าน URL
  2. พนักงานระดับช่าง (Technicians) สามารถเข้าถึงหน้ากากจัดการระดับแอดมินหรือเจ้าของร้านได้โดยไม่มีการตรวจสอบสิทธิ์หรือเด้งกลับ (Bypass Middleware Role Gates)
* **ข้อเสนอแนะในการแก้ไข:**  
  แก้ไขการตรวจสอบพาธใน `src/middleware.ts` ให้ตรงกับความจริง:
  ```typescript
  if (!user && (pathname.startsWith("/owner") || pathname.startsWith("/branch") || pathname.startsWith("/tech"))) {
    return NextResponse.redirect(new URL("/login", request.url));
  }
  ```

---

### 🚨 BUG 2: ปัญหาการหักลบสต็อกซ้ำซ้อนและการบันทึกบัญชีเคลื่อนไหวสองครั้ง (Double-Deduction & Duplicate Stock Movements)
* **ไฟล์ที่มีปัญหา:** `src/app/api/v1/jobs/[id]/parts/route.ts` ร่วมกับ `supabase/migrations/20260601000000_init_schema.sql`
* **รายละเอียดปัญหา:**  
  เมื่อมีการเพิ่มพาร์ทอะไหล่ลงในใบงานซ่อมผ่าน API POST ย่อย ตรรกะในระดับโค้ด TypeScript ได้ทำการ:
  1. หักลบจำนวนสินค้าคงคลังในตาราง `inventories` (บรรทัดที่ 95 ใน `route.ts`)
  2. สร้างรายการบัญชีสต็อกเคลื่อนไหวในตาราง `stock_movements` (บรรทัดที่ 105 ใน `route.ts`)
  3. เพิ่มข้อมูลลงในตารางอะไหล่ใบงานซ่อม `job_parts` (บรรทัดที่ 128 ใน `route.ts`)  
  
  **อย่างไรก็ตาม** ในฐานข้อมูลระดับ PostgreSQL มีการสร้างตัวทริกเกอร์อัตโนมัติ `tr_job_parts_inserted AFTER INSERT ON public.job_parts` ซึ่งทำหน้าที่หักสต็อกและสร้างรายการเคลื่อนไหวสต็อก `'job_consumption'` ซ้ำอีกครั้งเมื่อเกิดการบันทึกรายการในตาราง `job_parts`
* **ผลกระทบ:**  
  สต็อกสินค้าจะถูก**หักออกสองเท่า (Double Deduction)** จากยอดใช้จริง และเกิดประวัติการเบิกใช้อะไหล่ซ้ำสองรายการในสต็อกเคลื่อนไหว (Duplicate Ledger Entries) ส่งผลให้ตัวเลขสินค้าคงคลังจริงคลาดเคลื่อนอย่างรุนแรง
* **ข้อเสนอแนะในการแก้ไข:**  
  ควรลบโค้ดหักสต็อกและบันทึกสต็อกเคลื่อนไหวระดับ TypeScript ในไฟล์ API `route.ts` ทิ้งไป (ให้เขียนลงตาราง `job_parts` โดยตรง) เพื่อมอบหน้าที่ให้เป็นของ Database Trigger ในฐานข้อมูลแต่เพียงผู้เดียวเพื่อความสอดคล้องกันของระบบธุรกรรม (Atomicity)

---

## 3. ปัญหาระดับความปลอดภัยและตรรกะทางธุรกิจระดับกลางและต่ำ (Medium & Low Priority Issues)

### ⚠️ BUG 3: ความเสี่ยงการเกิดปัญหา Recursive ในนโยบายระดับแถวข้อมูลของตารางโปรไฟล์ (Potential RLS Infinite Recursion on `profiles`)
* **ไฟล์ที่มีปัญหา:** `supabase/migrations/20260601000000_init_schema.sql`
* **รายละเอียดปัญหา:**  
  นโยบาย RLS `select_profiles` บนตาราง `public.profiles` เรียกใช้ฟังก์ชัน `public.is_owner()`  
  ซึ่งภายในฟังก์ชัน `public.is_owner()` มีการรันคำสั่งคิวรีดึงข้อมูลจากตาราง `public.profiles` เพื่อตรวจสอบบทบาท:
  `SELECT role = 'owner' FROM public.profiles WHERE id = auth.uid() AND deleted_at IS NULL`  
  หากโปรไฟล์ไม่ได้อยู่ในสถานะข้ามผ่าน (Bypass RLS) อาจทำให้เกิดปัญหาวงวนไม่สิ้นสุด (Infinite Recursion) เมื่อเรียกใช้งานคำสั่งดึงข้อมูลโปรไฟล์โดยตรง
* **ผลกระทบ:** อาจเกิดข้อผิดพลาดฐานข้อมูลในระดับโปรดักชัน `infinite recursion detected in policy` คอนเนคชันฐานข้อมูลถูกตัด และหน้าเว็บค้าง
* **ข้อเสนอแนะในการแก้ไข:**  
  ปรับวิธีเช็กสิทธิ์ระดับเจ้าของร้านจากค่าเมทาดาต้าของ JWT โทเค็นโดยตรง `auth.jwt() ->> 'role' = 'owner'` เพื่อเลี่ยงการคิวรีตารางตัวมันเองซ้ำ

---

### ⚠️ BUG 4: ข้อผิดพลาดการคำนวณวันสิ้นสุดการรับประกันในเดือนที่มีจำนวนวันไม่เท่ากัน (Date Arithmetic Overflow)
* **ไฟล์ที่มีปัญหา:** `src/app/api/v1/warranties/route.ts` (บรรทัดที่ 165)
* **รายละเอียดปัญหา:**  
  การคำนวณวันหมดอายุของใบรับประกันในระบบใช้ตรรกะมาตรฐาน Javascript:
  `endDate.setMonth(startDate.getMonth() + duration_months);`  
  เมื่อลงทะเบียนรับประกันในวันสิ้นเดือนของเดือนที่มี 31 วัน (เช่น 31 มกราคม) และกำหนดระยะเวลา 1 เดือน ผลลัพธ์จะล้นสิทธิออกไปยังวันที่ 2 หรือ 3 มีนาคม (เนื่องจากกุมภาพันธ์ไม่มีวันที่ 31) แทนที่จะสิ้นสุดที่ 28 หรือ 29 กุมภาพันธ์
* **ผลกระทบ:** ลูกค้าได้รับระยะเวลารับประกันที่คลาดเคลื่อนจากความเป็นจริง
* **ข้อเสนอแนะในการแก้ไข:**  
  เขียนฟังก์ชันแคลมป์วันสิ้นเดือน (Clamping logic) เพื่อจำกัดวันหมดอายุไม่ให้เกินวันสุดท้ายของเดือนปลายทาง

---

### ⚠️ BUG 5: การจ่ายเงินเกินกว่ายอดค้างชำระโดยไม่มีการบล็อกตรวจสอบ (Invoice Overpayment Integrity Issue)
* **ไฟล์ที่มีปัญหา:** `src/app/api/v1/payments/route.ts` (บรรทัดที่ 68)
* **รายละเอียดปัญหา:**  
  ฟังก์ชันบันทึกการรับชำระเงินยอมรับจำนวนเงินใดๆ ที่ส่งเข้ามา หากส่งจำนวนเงินเกินกว่ายอดคงค้าง (`amount_due`) ระบบจะหักยอดค้างเหลือ 0 ผ่าน `Math.max(0, ...)` แต่จะเซฟยอดเงินสดที่รับมาจริงในตารางบัญชี `payments` เต็มจำนวน
* **ผลกระทบ:** ยอดเงินรับรวมในตารางการจ่ายเงินจะสูงกว่ามูลค่าสินค้าจริงในอินวอยซ์ ทำให้ไม่สามารถกระทบยอดบัญชีเงินสดได้ (Accounting Discrepancy)
* **ข้อเสนอแนะในการแก้ไข:**  
  เพิ่มการตรวจสอบ Zod หรือตรรกะเช็กใน API `payments/route.ts` ว่าหากยอดโอนชำระส่งมาสูงเกินกว่า `invoice.amount_due` ให้ปฏิเสธการทำรายการกลับทันที (ยกเว้นระบบมัดจำแยก)

---

### ⚠️ BUG 6: ขาดระบบสร้างข้อมูลโปรไฟล์อัตโนมัติเมื่อเพิ่มบัญชีผู้ใช้งาน (No Auto-Profile Database Trigger)
* **ไฟล์ที่มีปัญหา:** `supabase/migrations/20260601000000_init_schema.sql`
* **รายละเอียดปัญหา:**  
  ตาราง `public.profiles` ผูกความสัมพันธ์แบบ 1:1 กับบัญชีผู้ใช้ระบบจริงของ Supabase (`auth.users`) แต่ในไฟล์ไมเกรชันฐานข้อมูลกลับไม่มีการผูกฟังก์ชัน Trigger เพื่อก็อปปี้หรือสร้างข้อมูลพื้นฐานโปรไฟล์พนักงานทันทีหลังลงทะเบียนผ่าน Supabase Auth
* **ผลกระทบ:**  
  พนักงานใหม่ที่สมัครผ่านระบบหรือสร้างบัญชีใหม่จะไม่สามารถเข้าใช้งานระบบได้ เนื่องจากคิวรี `requireApiAuth` ไม่พบบันทึกโปรไฟล์ที่สอดคล้องใน `public.profiles`
* **ข้อเสนอแนะในการแก้ไข:**  
  เพิ่มตัวทริกเกอร์ `AFTER INSERT ON auth.users` เพื่อดึงอีเมลและชื่อมาสร้างเป็นโปรไฟล์เริ่มต้นโดยอัตโนมัติ

---

## 4. รายการตัวประมวลผลคำสั่ง API ที่สูญหาย (Missing API Handlers according to V3 Spec)

จากการเปรียบเทียบระหว่างเอกสาร **API Specification V3** กับตัวนำทางจริงในไดเรกทอรี `src/app/api/v1` พบว่ามีโมดูลและ API สำคัญที่ขาดหายไปดังนี้ครับ:

1. **ระบบเช็คเอาท์หน้าร้าน (POS Module):**
   * 🛑 **`POST /api/v1/pos/checkout`** (ขาดหายไปโดยสิ้นเชิง ไม่มีพาธ `src/app/api/v1/pos`)
2. **ระบบบันทึกเวลาเข้าออกงาน (Attendance Module):**
   * 🛑 **`POST /api/v1/attendance/clock-in`** (ไม่มีพาธ `src/app/api/v1/attendance`)
   * 🛑 **`PUT /api/v1/attendance/clock-out`** (ไม่มีพาธ `src/app/api/v1/attendance`)
3. **รายงานการลงเวลาเข้างาน (Attendance Report API):**
   * 🛑 **`GET /api/v1/reports/attendance`** (ขาดหายไปจากรายงานวิเคราะห์อื่น โดยระบบมีเพียง inventory, jobs, revenue, warranties เท่านั้น)
4. **ระบบจัดการตั้งค่าระดับสาขา (Settings Module):**
   * 🛑 **`GET /api/v1/settings/branch`** (ไม่มีพาธ `src/app/api/v1/settings`)
   * 🛑 **`PUT /api/v1/settings/branch`** (ไม่มีพาธ `src/app/api/v1/settings`)

---

## 5. แผนการตรวจสอบและปรับปรุงประสิทธิภาพความเร็ว (Performance Bottlenecks Audit)

1. **คิวรีรายงานวิเคราะห์ข้อมูล (Analytical Aggregations):**  
   ในปัจจุบันรายงาน Phase 6 ใช้คำสั่งคิวรี join ฐานข้อมูลแบบสด (On-the-fly) แม้ว่าจะมีการเพิ่มอินเด็กซ์ (Indexes) รองรับความเร็วไว้เป็นอย่างดี แต่หากปริมาณธุรกรรมสูงขึ้นในหลักแสนเรคคอร์ด คิวรีดึงข้อมูลรายงานผ่าน API ย่อยอาจส่งผลกระทบต่อความเร็วระบบ
   * *แผนปรับปรุง:* ควรทำตารางเก็บยอดสะสมรายวัน (Daily aggregated caching table) ในอนาคต
2. **การลบข้อมูลแบบนุ่มนวล (Soft Deletes Filtering):**  
   ทุกตารางหลักมีการใช้ `.is("deleted_at", null)` ซึ่งระบบเพิ่มอินเด็กซ์แบบเงื่อนไข (`WHERE deleted_at IS NULL`) ไว้รอบรับเรียบร้อยแล้ว ถือว่ามีประสิทธิภาพยอดเยี่ยมมากในเฟสนี้

---

*หมายเหตุ: รายงานนี้จัดทำขึ้นเพื่อนำเสนอรายการข้อดีข้อเสีย และช่องโหว่ความเสี่ยงระดับองค์กรโดยละเอียดตามข้อกำหนดโดยไม่มีการเพิ่มฟังก์ชันใหม่หรือดัดแปลงโครงสร้างสถาปัตยกรรมทางลัดใดๆ เพื่อให้วิศวกรรวมระบบสามารถนำไปปรับใช้อย่างถูกวิธีและปลอดภัยสูงสุดครับ!*
