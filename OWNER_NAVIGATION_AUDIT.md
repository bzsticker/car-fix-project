# รายงานการตรวจสอบระบบการนำทางแดชบอร์ดเจ้าของร้าน (OWNER_NAVIGATION_AUDIT.md)

**โครงการ:** Den Modify Management System  
**ประเภทรายงาน:** รายงานตรวจสอบเมนูแดชบอร์ดส่วนควบคุม (Owner Navigation Audit Report)  
**ปัญหาที่พบ:** ปุ่มเมนู Sidebar ของผู้จัดการ/เจ้าของกิจการ (Owner) รายงานความผิดพลาด 404 หลายรายการเมื่อคลิกเปิดใช้งาน ได้แก่ `/owner/branches`, `/owner/roster`, `/owner/stock` และ `/owner/reports`

---

## 1. ผลการตรวจเช็คระบบไฟล์จริง (Physical Filesystem Verification)

จากการตรวจสอบแบบสแกนโฟลเดอร์เชิงกายภาพลึกภายใต้ไดเรกทอรี `src/app/(dashboard)/owner/` พบผลลัพธ์ที่น่าประหลาดใจดังนี้:

```text
src/app/(dashboard)/owner/
└── page.tsx            <-- แดชบอร์ดภาพรวม (/owner) [มีอยู่จริง]
```

### รายการโฟลเดอร์และหน้าเพจที่ขาดหายไป (Missing Components):
1. **`branches` (ข้อมูลสาขา):** **ไม่มีโฟลเดอร์นี้อยู่จริง** ทำให้ลิงก์ `/owner/branches` ตอบสนองรหัส 404
2. **`roster` (สถิติการลงเวลาทำงานพนักงาน):** **ไม่มีโฟลเดอร์นี้อยู่จริง** ทำให้ลิงก์ `/owner/roster` ตอบสนองรหัส 404
3. **`stock` (คลังสินค้าส่วนกลาง):** **ไม่มีโฟลเดอร์นี้อยู่จริง** ทำให้ลิงก์ `/owner/stock` ตอบสนองรหัส 404
4. **`reports` (ระบบรายงานและวิเคราะห์ภาพรวม):** **ไม่มีโฟลเดอร์นี้อยู่จริง** ทำให้ลิงก์ `/owner/reports` ตอบสนองรหัส 404

*(หมายเหตุ: ถึงแม้ว่าหน้าจอจะแสดงข้อมูลการลิงก์เมนูใน Sidebar แต่องค์ประกอบหน้าวิวเพจของฝั่งผู้ดูแลเจ้าของร้านยังไม่ได้รับการจัดตั้งโครงสร้างโฟลเดอร์ขึ้นมาเพื่อจับคู่เส้นทางของ Next.js)*

---

## 2. การตรวจสอบสถานะเส้นทางในสารบบระบบ (Route Audit Matrix)

| ปลายทางที่เข้าถึง | สัญญาเส้นทางในไซด์บาร์ | การมีอยู่จริงในระบบไฟล์ (Filesystem) | ผลลัพธ์ในบราวเซอร์ | สถานะปัญหานำทาง |
|---|---|---|---|---|
| **Overview (หน้าแรก)** | `/owner` | `src/app/(dashboard)/owner/page.tsx` | **โหลดผ่าน 200 OK** | **ปกติ** |
| **Branches (สาขา)** | `/owner/branches` | *ไม่มีโฟลเดอร์ย่อย* | **404 Not Found** | **มีปัญหา (Broken)** |
| **Roster Logs (ลงเวลา)** | `/owner/roster` | *ไม่มีโฟลเดอร์ย่อย* | **404 Not Found** | **มีปัญหา (Broken)** |
| **Global Stock (คลังสินค้า)** | `/owner/stock` | *ไม่มีโฟลเดอร์ย่อย* | **404 Not Found** | **มีปัญหา (Broken)** |
| **Reports (วิเคราะห์ผล)** | `/owner/reports` | *ไม่มีโฟลเดอร์ย่อย* | **404 Not Found** | **มีปัญหา (Broken)** |

---

## 3. ผลวิเคราะห์ความเกี่ยวเนื่องข้ามโมดูล (Cross-Module Analysis)

จากการประเมินโครงสร้างส่วนควบคุมสาขา (`src/app/(dashboard)/branch/`) พบฟังก์ชันที่มีการสะกดคล้ายคลึงกัน แต่อ้างอิงพาร์ทที่ไม่สามารถทดแทนกันได้ด้วย RLS/Branch Isolation:
- **`reports` (รายงาน):** มีโฟลเดอร์ `src/app/(dashboard)/branch/reports` อยู่จริง แต่เป็นสถิติ **ระดับสาขา** ซึ่งไม่เหมาะกับบัญชี `owner` ที่ต้องการชมความเคลื่อนไหวทางการเงินและกำไรแบบ **ข้ามสาขา (Global Multi-branch Overview)**
- **`inventory` (คลัง):** มีโฟลเดอร์ `src/app/(dashboard)/branch/inventory` อยู่จริง แต่เป็นระบบการลงบัญชีระดับสาขา และไม่มีระบบตรวจสอบสินค้าโดยรวมแยกตามคลังของเจ้าของร้าน

ดังนั้น ลิงก์ทั้งหมดของผู้ดูแลระบบด้านบนนี้จึงเป็น **ลิงก์หน้าย่อยใหม่ทั้งหมด** ที่จำเป็นต้องจัดทำโครงสร้างหน้าเพจขึ้นมารองรับเพื่อให้สอดรับกับนโยบายระบบควบคุม HQ Board

---

## 4. ข้อเสนอแนะแนวทางแก้ไขและสถาปัตยกรรมย่อย (Recommended Fixes)

เพื่อรักษาภาพลักษณ์และดีไซน์ระดับพรีเมียมของระบบบริหารอู่ **Den Modify** ให้มีความสวยงาม ทันสมัย และตอบโจทย์การประเมินผลงาน เราขอเสนอให้สร้างหน้าเพจจำลองและสคริปต์ควบคุม (**Premium Placeholder Pages**) ที่เชื่อมต่อดึงข้อมูลจริงจาก Supabase ดำเนินการดังนี้:

### ก. การเปิดใช้งานเส้นทางและจัดทำเพจ:
เราสมควรเข้าไปสร้างโฟลเดอร์ย่อยและไฟล์เพจแสดงผลภายใต้ `src/app/(dashboard)/owner/` ดังพิกัดพาร์ทเหล่านี้:

1. **[NEW] โครงสร้างหน้าสาขาภาพรวม:** `src/app/(dashboard)/owner/branches/page.tsx`
   - **แนวทางดีไซน์:** จัดแสดงข้อมูลการเปรียบเทียบสาขากรุงเทพฯ (HQ) และสาขาปทุมธานี (Outpost) พร้อมสถิติจำนวนใบงานซ่อม สัดส่วนรายได้ และสัญญลักษณ์แสดงสถานะออนไลน์แบบ Real-time
2. **[NEW] โครงสร้างหน้าประวัติการเข้างานพนักงาน:** `src/app/(dashboard)/owner/roster/page.tsx`
   - **แนวทางดีไซน์:** ตารางประวัติลงบันทึกเวลาทำงานของช่างเทคนิคจากทั้ง 2 สาขา ดึงข้อมูลดิบจากตาราง `public.attendance_logs` มาจัดระเบียบตารางพร้อมรูปภาพถ่ายเซลฟีแสดงตัวตนและพิกัด GPS
3. **[NEW] โครงสร้างหน้ารายงานคลังสินค้าส่วนกลาง:** `src/app/(dashboard)/owner/stock/page.tsx`
   - **แนวทางดีไซน์:** แผงควบคุมจำนวนอะไหล่และชิ้นงาน (3M Wrap, Gyeon, exhaust Akrapovic) ที่กระจายตัวอยู่ตามสาขา ดึงข้อมูลคลังจากตาราง `public.inventories` เพื่อแสดงระบบแจ้งเตือนเมื่อสินค้าใกล้หมดส่วนกลาง
4. **[NEW] โครงสร้างหน้ารายงานกำไรการวิเคราะห์บอร์ดใหญ่:** `src/app/(dashboard)/owner/reports/page.tsx`
   - **แนวทางดีไซน์:** แผนภูมิสรุปยอดขาย การวิเคราะห์ผลงานช่าง (Job Performance) และสัดส่วนหมวดสินค้าที่ทำกำไรสูงสุด เพื่อสนับสนุนการตัดสินใจของเจ้าของร้าน

---

## 5. แผนการดำเนินการลำดับขั้นถัดไป (Action Plan)

1. รอรับการตัดสินใจและอนุมัติแนวทางสร้างหน้าเพจ HQย่อย ทั้ง 4 โฟลเดอร์จากผู้ใช้งาน
2. ทันทีที่ได้รับการอนุมัติ จะเข้าจัดทำไฟล์ `page.tsx` ของแต่ละโมดูลอย่างละเอียดรอบคอบด้วยกลยุทธ์แก้วโปร่งแสง (Glassmorphic Glass-Cards) และ Harmony Color Palette ของแบรนด์ Den Modify
3. ดำเนินการประเมินตรวจสอบ Lint และ Unit test ของโค้ดเบสทั้งหมดเพื่อให้มั่นใจว่าแอปพลิเคชันยังเสถียร 100%

*(สอดคล้องตามข้อตกลงและข้อกำหนด ขณะนี้ยังไม่มีการแก้ไข ดัดแปลง หรือเขียนสคริปต์ลงโค้ดเบส เพื่อรอการอนุมัติสิทธิแนวทางการสร้างหน้าเว็บย่อยจากคุณ)*
